# audio-controller
Remote volume controller: mobile app + FastAPI backend to control Windows audio (master volume, per-app volume, mute) over WiFi, with additional features such as a soundboard and custom presets.
